# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Database & secrets
data/
.env

# NiceGUI storage
.nicegui/

# IDE
.vscode/
.idea/

# Temp files
tmp_*.txt
~$*.xlsx
~$*.docx
*.sqbpro

# Logs
logs/

# Report outputs
BaoCaoHauKiem/
MCB.xlsx

# Offline standalone package (build output — regenerate via scripts/build_offline_package.ps1)
dist_offline/

# Dữ liệu tài chính thật / thư mục scratch verify — KHÔNG commit (audit 2026-08-05: ~50GB
# nằm untracked ở gốc dự án, chưa có gitignore nào chặn, rủi ro commit nhầm toàn bộ).
# Xóa/di chuyển các mục này CẦN quyết định riêng, không tự ý xóa — xem
# feedback_ready_for_ux_handoff / project_scratch_cleanup_reminder trong memory.
/Cham ILO1000/
/Chấm 459901/
/DoichieuSongphuong/
/citad -hub/
/Đối chiếu ACH/
/Đối chiếu ACH.zip
/_scratch_milestone_F1/
/_scratch_verify_1112/
/_scratch_verify_167/
/_scratch_verify_22_07_session_null_br/
/_scratch_verify_dien_rong_25_07/
/_scratch_verify_session_null_br/

# Local artifacts / dữ liệu test (không đẩy lên repo)
/Dữ liệu test/
/deploy-test.bat
/KSNB - HDSD phan mem ban giao chung tu.docx
/Bắt đầu trên máy mới.txt

# Tên file tiếng Việt trên đĩa ở dạng Unicode TRỘN (ư + dấu rời), không khớp
# dạng dựng sẵn viết ở trên → git bỏ qua luật. Ghi thêm đúng dạng thật:
/Bắt đầu trên máy mới.txt
/Bắt đầu trên máy mới.txt
/Dữ liệu test
/Dữ liệu test

# Cấu hình Claude Code riêng từng máy — chứa đường dẫn tuyệt đối ổ G:, thư mục temp
# theo UUID phiên, lệnh scratch dùng một lần. Phần dùng chung nằm ở .claude/settings.json.
.claude/settings.local.json

# Scratch artifacts sinh trong lúc verify + file dữ liệu rời ở gốc.
# Từ 2026-08-10 repo được commit thường xuyên hơn để đồng bộ phiên cloud/điện thoại
# → phải chặn sẵn, tránh lọt dữ liệu thật lên remote.
/scratch_*.txt
/scratch_*.csv
/scratch_*.json
/nul
/20260706.xlsx
